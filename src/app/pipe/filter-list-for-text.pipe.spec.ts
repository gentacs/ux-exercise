import { FilterListForTextPipe } from './filter-list-for-text.pipe';

describe('FilterListForTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListForTextPipe();
    expect(pipe).toBeTruthy();
  });
});
